#include "file.h"
#include <stdio.h>
#include <stdlib.h>

unsigned int ReadLine(int filefb, char *buff)
{
    if(filefb < 0){
        return -1;
    }
    
    if(NULL == buff){
        return -1;
    }
	
	return 0;
}
