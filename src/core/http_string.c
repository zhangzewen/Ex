#include "http_string.h"
#include <stdio.h>
#include <stdlib.h>
char *get_string(const char *src, const char *StartWith, const char *EndWith)
{
	if(NULL == src){
		printf("the src is NULL!\n");
		return NULL;
	}
	
	return NULL;
}
